# Production dependencies for mcp-location-server
# These dependencies are also defined in pyproject.toml [project.dependencies]
# This file exists for compatibility with tools like Snyk that expect requirements.txt

mcp>=1.0.0
httpx>=0.25.0
pydantic>=2.0.0